# @homeofthings/node-shell

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build node-shell` to build the library.

## Running unit tests

Run `nx test node-shell` to execute the unit tests via [Jest](https://jestjs.io).
