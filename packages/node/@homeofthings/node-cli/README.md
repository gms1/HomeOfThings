# @homeofthings/node-cli

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build node-cli` to build the library.

## Running unit tests

Run `nx test node-cli` to execute the unit tests via [Jest](https://jestjs.io).
